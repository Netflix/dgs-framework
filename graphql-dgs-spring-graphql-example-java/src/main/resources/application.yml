spring:
  application:
    name: dgs-spring-graphql-example

  graphql:
    graphiql:
      enabled: true
    websocket:
      path: /graphql

management:
  endpoints:
    web:
      exposure:
        include:
          - metrics
debug: true